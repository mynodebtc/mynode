[Service]
AppArmorProfile=
# Enable or disable hidden services before every start. "+" skips the unit's
# read-only filesystem and ProtectHome sandboxing, which would block the script.
ExecStartPre=+/usr/bin/mynode_gen_tor_config.sh
