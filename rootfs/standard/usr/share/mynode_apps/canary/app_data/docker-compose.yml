version: "3.8"

services:
  backend:
    image: schjonhaug/canary-backend:v1.6.2
    user: "${CANARY_HOST_UID}:${CANARY_HOST_GID}"
    network_mode: host
    restart: unless-stopped
    stop_grace_period: 30s
    volumes:
      - /mnt/hdd/mynode/canary:/app/data
    env_file:
      - /mnt/hdd/mynode/canary/canary.env
    environment:
      CANARY_DATA_DIR: /app/data
      CANARY_ELECTRUM_URL: tcp://127.0.0.1:50001
      CANARY_NETWORK: mainnet
      CANARY_MODE: self-hosted
      CANARY_BIND_ADDRESS: 127.0.0.1:3004
      FRONTEND_URL: http://mynode.local:3005

  frontend:
    image: schjonhaug/canary-frontend:v1.6.2
    network_mode: host
    restart: unless-stopped
    stop_grace_period: 30s
    depends_on:
      - backend
    environment:
      API_URL: http://127.0.0.1:3004
      PORT: "3005"
